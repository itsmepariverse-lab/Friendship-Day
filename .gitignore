backup_v1/
.gemini/
*.webp
*.png.png
*.jpg.jpg
node_modules/
.DS_Store

# Radio station MP3s (~2GB of commercial music) — kept local, never committed.
# The player reads them from disk; the generated manifest is committed instead.
Friendship Day/assets/music/*/

# Private WhatsApp export — never commit real chat history to a public repo.
*WhatsApp Chat with*.txt

# Local env vars (API keys) — never commit secrets.
.env
.env.local
.vercel

# Local-only Python scripts (upload/conversion tools, not needed on Vercel)
r2_hls_upload.py
r2_upload.py
r2_cors_setup.py
hls_converter.py
hf_upload.py
convert_srt.py
